{
  "name": "@luxel/infra-cloudflare",
  "version": "0.0.0",
  "private": true,
  "description": "Pulumi (TypeScript) IaC for the Cloudflare zone: DNS + Email Routing.",
  "main": "index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "preview": "pulumi preview",
    "up": "pulumi up",
    "export": "node scripts/export-cloudflare.mjs",
    "import": "node scripts/gen-imports.mjs"
  },
  "dependencies": {
    "@pulumi/cloudflare": "^6.9.0",
    "@pulumi/pulumi": "^3.201.0"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "typescript": "^5.6.2"
  }
}
