name: Worker deploy

# Push to main touching the worker (or manual dispatch) → `wrangler deploy`.
# Secrets set with `wrangler secret put` are NOT touched by a deploy; only the
# code, the bindings and the [vars] in wrangler.toml are published.
#
# Needs the CLOUDFLARE_API_TOKEN repo secret. Without it the job skips green,
# and the worker keeps running its previously deployed code.
on:
  push:
    branches: [main]
    paths: ['workers/whatsapp/**', '.github/workflows/worker-deploy.yml']
  workflow_dispatch: {}

concurrency:
  group: worker-deploy
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Guard — require CLOUDFLARE_API_TOKEN
        id: guard
        env:
          TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        run: |
          if [ -z "$TOKEN" ]; then
            echo "::notice::CLOUDFLARE_API_TOKEN not set — skipping. Add it to auto-deploy the worker."
            echo "skip=true" >> "$GITHUB_OUTPUT"
          fi

      - uses: pnpm/action-setup@v4
        if: steps.guard.outputs.skip != 'true'
        with:
          standalone: true

      - uses: actions/setup-node@v4
        if: steps.guard.outputs.skip != 'true'
        with:
          node-version-file: '.nvmrc'
          cache: pnpm

      - run: pnpm install --frozen-lockfile
        if: steps.guard.outputs.skip != 'true'

      - name: Typecheck the worker
        if: steps.guard.outputs.skip != 'true'
        run: pnpm --filter @luxel/whatsapp-worker typecheck

      - name: Deploy
        if: steps.guard.outputs.skip != 'true'
        run: pnpm --filter @luxel/whatsapp-worker deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
